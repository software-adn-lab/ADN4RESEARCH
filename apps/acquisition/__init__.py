"""Acquisition app package.

Scaffolded package for the acquisition module. Implementations live in
`domain/` and `components/` subpackages.
"""

default_app_config = "apps.acquisition.apps.AcquisitionConfig"
