from django.urls import path

app_name = "acquisition"

urlpatterns = [
    # Add acquisition endpoints here, e.g. path('search/', views.search)
]
from django.urls import path
from . import views

app_name = 'acquisition'

urlpatterns = [
    # Add your URL patterns here
]
