[project]
name = "tesis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.7",
]
[tool.behave]
junit = true
junit_directory = "tests"
paths = [
    "tests/design/",
    "tests/interpretation/",
    "tests/extraction/",
    "tests/selection/",
    "tests/acquisition/",
]