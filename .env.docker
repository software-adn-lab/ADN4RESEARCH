# =============================================================================
# DOCKER ENVIRONMENT CONFIGURATION
# Copy this to .env when using Docker: cp .env.docker .env
# =============================================================================

# Django Core
SECRET_KEY=django-insecure-oxq-k)&z4v0ip6rtm)3c%44(-7q1@_ddfsk41+_)yg6zo9zo3i
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,web
DJANGO_PORT=8000

# PostgreSQL (Docker service name)
DB_NAME=adn4research
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Redis (Docker service name)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123
REDIS_DB=0

# MinIO Storage (Docker service name)
USE_S3=True
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_STORAGE_BUCKET_NAME=adn4research
AWS_S3_ENDPOINT_URL=http://minio:9000
AWS_S3_REGION_NAME=us-east-1
AWS_S3_VERIFY=False

# MinIO Ports
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
